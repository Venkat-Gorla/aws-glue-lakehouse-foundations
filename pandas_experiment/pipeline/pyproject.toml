[project]
name = "lakehouse-pandas"
version = "0.1.0"
description = "Add your description here"

authors = [
    { name = "Venkat Gorla" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
]

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
