[project]
name = "pandas-experiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lakehouse-pandas",
]

[tool.uv.sources]
lakehouse-pandas = { workspace = true, editable = true }
