[project]
name = "aws-glue-lakehouse-foundations"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "pandas_experiment/pipeline",
    "pandas_experiment",
]
